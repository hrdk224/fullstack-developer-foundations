// Arrow function is the shorter version of function

let sayHi = () => console.log('hello');
sayHi();

// MultiLine : just need to add braket

let sum = (a, b) => {
  result = a + b;
  return result;
};

console.log(sum(2, 4));
