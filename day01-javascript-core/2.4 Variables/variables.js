// Variables can be declred using let, var(old convention), const(cannot be changed once declared)

let message = 'hello World!';
message = 'Hi';
console.log(message);

const COLOR = 'Red';
console.log(COLOR);
